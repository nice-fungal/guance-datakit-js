import { ONE_HOUR, ONE_MINUTE } from '../helper/tools'
export var SESSION_TIME_OUT_DELAY = 2 * ONE_MINUTE;   // 4 * ONE_HOUR
export var SESSION_EXPIRATION_DELAY = 1 * ONE_MINUTE; // 15 * ONE_MINUTE
export var SESSION_STORE_KEY = '_gc_s'
